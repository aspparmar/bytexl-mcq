#include <stdio.h>
#include <stdlib.h>
long long sumEvenIndices(const int *arr, int n) {
\n    // TODO: implement using pointer arithmetic\n    return 0;\n}\n\nint main() {\n    int n;\n    if (scanf("%d", &n) != 1) return 0;\n    int *arr = (int*)malloc(n * sizeof(int));\n    for (int i = 0; i < n; ++i) {\n        scanf("%d", &arr[i]);\n    }\n    long long ans = sumEvenIndices(arr, n);\n    printf("%lld", ans);\n    free(arr);\n    return 0;\n}